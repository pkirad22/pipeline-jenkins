pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building project from GitHub'
            }
        }

        stage('Test') {
            steps {
                echo 'Running tests'
            }
        }
    }
}